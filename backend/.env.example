# Desarrollo
NODE_ENV=development
WEB_SERVER_PORT=3000
SWAGGER_ENABLED=true

# CORS
FRONTEND_URL=http://localhost:5173
# Origin adicional permitido por CORS (p. ej. el front en la LAN). Opcional; vacío = se ignora.
LOCAL_DIR_URL=http://TU_IP_LOCAL:5173

# IA local (Ollama)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b
OLLAMA_VISION_MODEL=gemma3:4b
# Los modelos locales pueden tardar varios minutos. (240000 = 4 min).
OLLAMA_TIMEOUT_MS=180000
