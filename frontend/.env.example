# Opcional. Por defecto el frontend usa el MISMO host que lo sirve, en el puerto 3000
# (funciona en localhost y desde el móvil/LAN sin tocar nada).
# Descoméntalo solo si tu backend está en otro host o puerto:
# VITE_BACKEND_BASE_URL=http://localhost:3000
